# Algorithmic Architecture of the Interactive Multiverse Loop Engine

This document provides the technical overview and execution parameters for `interactive.py`. The engine simulates a multi-generational Conformal Cyclic Cosmology (CCC) cascade model under background-independent Loop Quantum Gravity (LQG) constraints, starting strictly at Iteration 0.

## 1. Input Parameters & Core Dynamics

The interactive pipeline processes two primary user-defined variables to calculate the trajectory of the observer metric:

*   **Total Iterations (Max Aeons):** Regulates the sequential execution of global conformal resets. Each completed cycle increments the chronological framework, forcing the system to re-evaluate structural seeding invariants.
*   **Timeline Displacement (Gyr):** Quantifies the perceived temporal delay for internal mass-bearing observers during the conformally instantaneous transition across the Event 3 boundary.

## 2. Stochastic Interception & Core Piracy Mechanics

During multi-generational cascades (where Iteration > 0), the engine integrates non-global Pathway 2 events occurring in adjacent domains. 

$$\hat{Q}|\Psi_{\text{Multiversum}}\rangle = 0$$

Instead of tracking the full evolutionary path of the severed daughter sub-manifold, the algorithm models the non-local gravitational footprint acting upon the primary channel. This manifests as a localized metric drainage (Core Piracy), mathematically evacuating mass invariants through the topological perforation. The baseline mass integrity is dynamically recalibrated via the relation:

$$M_{\text{recalibrated}} = M_{\text{baseline}} - (1,250,000 \times \text{Iteration})$$

The parallel lineage terminates instantly post-drainage, ensuring the observer remains structurally bound to the primary cosmological matrix without violating global CPT invariance.
